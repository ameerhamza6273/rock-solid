import { storyblokButton, storyblokHeadings, storyblokAsset } from './storyblokMain';

export interface storyblokFindOut{
    image: storyblokAsset;
    headings: storyblokHeadings[];
    action: storyblokButton[];
}