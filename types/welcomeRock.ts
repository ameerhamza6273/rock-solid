import { storyblokAsset, storyblokHeadings} from './storyblokMain';

export interface welcomeRock{
    headings: storyblokHeadings[];
    text1: string;
    text2: string;
    text3: string;
    text4: string;
    image: storyblokAsset;
}